main :: () {
    // ---------- Modulos --------------
    {
        assert(10 % 3 == 1);
        assert(10 % 2 == 0);
        assert(7 % 5 == 2);
        assert(-10 % 3 == -1);
    
        a := -123456789;
        b := 97;
        r := (a / b) * b + (a % b);
    
        assert(a == r);
    }
}