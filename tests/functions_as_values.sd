import "fmt";

main :: () {
    // Functions can be assigned to a new function
    {
        foo := () {
            Print("foo");
        };

        foo = () {
            Print("another foo");
        };

        foo(); // -> another foo
    }
}