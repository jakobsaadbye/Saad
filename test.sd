Fruit :: enum {
    mango,
    kiwi,
    watermelon,
    dragonfruit,
    pear,
    apple,
}

main :: () {

    print("%\n", Fruit.count);

    for name in Fruit.names {
        print("%\n", name);
    }

    for value in Fruit.values {
        print("%\n", cast(int) value);
    }

}