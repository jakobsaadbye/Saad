Ball :: struct {
    x:      float;
    y:      float;
    size:   float;
    vel_x:  float;
    vel_y:  float;
}

early_return :: (r: int) {
    if r > 10 {
        return;
    }

    print("r is = %", r);
}

main :: () {
    early_return(10);
}
