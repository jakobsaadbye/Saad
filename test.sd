Monster :: struct {
    name  : string;
    ugly  : bool;
    scary : bool;
}

HTTPStatus :: enum {
    ok = 200,
    bad_request = 400,
    unauthorized = 401,
}

main :: () {
    // monsters : []Monster = [
    //     .{"Mike", true, false},
    //     .{"Solomon", false, true},
    //     .{"Ralph", true, true},
    // ];
    // for m in monsters {
    //     print("%, ugly = %, scary = %", m.name, m.ugly, m.scary);
    // }

    // error1 := HTTPStatus.ok;
    // error2 := HTTPStatus.bad_request;
    // error3 := HTTPStatus.unauthorized;
    // print("Received http status: % and % and %", error1, error2, error3);
}
