Status :: enum {
    ok = 1,
    created,
    
    bad_request = 400,
    permission_denied,
    payment_required,
    forbidden,
    not_found,
    method_not_allowed = Status.bad_request + 5,
}

DEFAULT_STATUS :: Status.created;

main :: () {
    print(DEFAULT_STATUS);
}