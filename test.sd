extern "C" {
    printf :: (format: *u8, args: ...any);
}

foo :: (a: int, b: float, c: f64, d: u8, e: f64) {
    x := 69;
    printf(xx "%d-%f-%lf-%d-%lf\n", a, b, c, d, e);
    y := x + 3;
    printf(xx "%d-%d\n", x, y);
}

main :: () {
    foo(1, 2.5, 3.14, 255, 6.28);
}