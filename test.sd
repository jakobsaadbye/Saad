extern "C" {
    printf :: (format: *u8, args: ...any);
}

Vector4 :: struct {
    x, y, z, w: float;
}

Vector2 :: struct {
    x, y: float;
}

Mesh :: struct {
    points: [3]Vector2;
}

Entity :: struct {
    health: int;
    rotation: Vector4;
    mesh: [10]Vector2;
    mana: int;
}

main :: () {
    e := Entity{
        health = 100,
        rotation = Vector4{4, 9, 13, 15},
        mesh = [{1, 2}, {3, 4}]
    };
    
    v := e.mesh[1].x;

    printf(xx "%lf", v);

    // printf(xx "%d %d %f %f %f %f\n", e.health, e.mana, e.rotation.x, e.rotation.y, e.rotation.z, e.rotation.w);
}