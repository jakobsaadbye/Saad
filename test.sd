import "fmt";

main :: () {

    // We can create functions anywhere, even in arrays
    myFuncs := [
        () { Print("0"); },
        () { Print("1"); },
        () { Print("2"); },
    ];

    for func in myFuncs {
        func();
    }

};