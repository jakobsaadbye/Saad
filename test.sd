Vector2 :: struct {
    x: float;
    y: float;
}

Entity :: struct {
    pos: [2]Vector2; // offset = 0, elem_0 = 16
}

main :: () {

    // e := Entity{pos = [{1, 3}, {4, 9}]};

    // static := [1, 2, 3][0];
    // print("%", static);
    // dynamic : [..]int = [1, 2, 3];

    // for dynamic {
    //     print("%", it);
    // }

    // for static {
    //     print("%", it);
    // }
}