extern "C" {
    printf :: (format: *u8, args: ...any);
}

foo :: () -> int {
    return 69;
}

main :: () {
    a := 5.0;
    // c := foo();

    printf(xx "%f-%f-%f-%f-%f-%f\n", a, a, a, a, a, a);
}