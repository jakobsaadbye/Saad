Rectangle :: struct {
    x: float;
    y: float;
    width: float;
    height: float;
}

foo :: (r: *Rectangle) {
    if (r.x == 50) {
        return;
    }
    r.x += 100;
}

main :: () {
    r := Rectangle{50, 75, 100, 200};
    foo(&r);
}