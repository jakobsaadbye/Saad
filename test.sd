Color :: struct {
    r: u8;
    g: u8;
    b: u8;
    a: u8;
}

DrawText :: (text: string, posX: int, posY: int, fontSize: int, color: Color) -> Color {
    return color;
}

main :: () {
	color := Color{40, 60, 80, 100};
	print("%", color);
	// c := DrawText("Hellope", 69, 420, 24, color);
}