main :: () {
    result := factorial(2);
    print(result);
}

factorial :: (n: int) -> int {
    print(n);
    if (n == 0 || n == 1) {
        return 1;
    } else {
        return n * factorial(n - 1);
    }
}